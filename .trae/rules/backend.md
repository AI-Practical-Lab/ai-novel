---
description: 后端 API 与数据层开发规范
globs: "api/**/*, server/**/*, src/services/**/*"
alwaysApply: false
---
# 后端服务开发规范

## API 设计
- **风格**：遵循 RESTful 设计原则，使用语义化的 HTTP 方法和资源路径。
- **响应格式**：所有接口返回统一的响应包装结构：`{ code: number, data: T, message: string }`。
- **输入验证**：在处理任何请求逻辑前，**必须**使用 Zod、Joi 或类验证器对输入参数进行验证。
- **错误处理**：使用统一的错误处理中间件。抛出的业务错误必须包含可追踪的错误码和用户友好的信息。

## 数据与安全
- **数据库操作**：使用参数化查询或 ORM 以防止 SQL 注入。优化查询，避免 N+1 问题。
- **身份验证与授权**：所有需要认证的接口**必须**进行 token 校验。敏感操作必须进行角色或权限检查。
- **环境变量**：所有密钥、数据库连接字符串等敏感信息**必须**从环境变量读取，**严禁**硬编码在源码中。
- **日志记录**：在关键业务分支、错误捕获处添加结构化的日志（等级、场景、关键参数）。

## 代码结构
- **分层清晰**：遵循 Controller -> Service -> Repository/DAO 的分层模式。
- **异步处理**：优先使用 `async/await`，避免回调地狱。
- **函数单一职责**：每个函数只做一件事。将数据校验、业务逻辑、数据持久化分离。
